import type { PropsWithChildren } from 'react'

export const metadata = {
	title: 'Encrypt & Decrypt a String',
	description: 'Encrypt & Decrypt a String',
}

export default function Layout({ children }: PropsWithChildren<unknown>) {
	return children
}
