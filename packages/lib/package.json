{
  "name": "lib",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "lint": "eslint ./{src,tests}/*.ts",
    "test": "jest --coverage",
    "test:watch": "jest --coverage --watchAll",
    "test:ci": "npm run test"
  },
  "devDependencies": {
    "config": "workspace:*",
    "tsconfig": "workspace:*",
    "tsup": "5.11.13"
  }
}
